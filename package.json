{
  "name": "crm-serralheria-root",
  "version": "1.0.0",
  "description": "Sistema Operacional de Serralheria (SOS) - Monorepo",
  "scripts": {
    "bootstrap": "npm run install-backend && npm run install-frontend",
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --prefix frontend",
    "dev-backend": "npm run dev --prefix backend",
    "dev-frontend": "npm run dev --prefix frontend",
    "dev": "npm run dev-backend & npm run dev-frontend",
    "build": "npm run build --prefix frontend && rm -rf backend/public && cp -r frontend/dist backend/public",
    "start": "npm run start --prefix backend"
  }
}
